# fly.io deploy:  fly launch --no-deploy  then  fly deploy
app = "keel"
primary_region = "sin"                 # Singapore — near India
[build]
  dockerfile = "Dockerfile"
[env]
  KEEL_AUTH_REQUIRED = "1"
  KEEL_HTTPS = "1"
  PORT = "8347"
[http_service]
  internal_port = 8347
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1
  [[http_service.checks]]
    path = "/healthz"
    interval = "30s"
    timeout = "5s"
[mounts]
  source = "keel_data"
  destination = "/data"
# After deploy set secrets:
#   fly secrets set KEEL_SECRET_KEY=$(openssl rand -hex 32)
#   fly secrets set KEEL_SIGNING_KEY_PEM="$(keel keygen --quiet)"
#       ^ the Ed25519 authority key. Without it, a redeploy regenerates the key
#         and every certificate ever issued stops verifying.
#   fly secrets set KEEL_SIGNUP=invite KEEL_INVITE_CODE=...
#   fly secrets set KEEL_TRUSTED_PROXY=1     # Fly terminates TLS
#   fly secrets set GEMINI_API_KEY=...
