apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-keel
spec:
  type: {{ .Values.service.type }}
  selector: { app: keel }
  ports:
    - { port: {{ .Values.service.port }}, targetPort: 8347 }
